// import express from 'express';
// import { WalletController } from '../controllers/walletController';
// import { authMiddleware } from '../middleware/auth';

// const router = express.Router();
// const controller = new WalletController();

// router.post('/users', controller.createUser);
// router.post('/wallets/fund', authMiddleware, controller.fundWallet);
// router.post('/wallets/transfer', authMiddleware, controller.transferFunds);
// router.post('/wallets/withdraw', authMiddleware, controller.withdrawFunds);
// router.get('/wallets/balance', authMiddleware, controller.getBalance);
// router.get('/transactions', authMiddleware, controller.getTransactions);

// export { router };
